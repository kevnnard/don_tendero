<% if (user) { %>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarScroll">
      <ul class="navbar-nav ml-auto me-auto my-2 my-lg-0 navbar-nav-scroll" style="--bs-scroll-height: 100px;">
        <% if (user) { %>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/"> TU TIENDA,  <span><%= user.name_place %></span></a>
        </li>
        <% } %>
        <li class="nav-item">
          <a class="nav-link" href="/about"> Sobre Don tendero</a>
        </li>
        <% if (user) { %>
        <!-- <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarScrollingDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Menu
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarScrollingDropdown">
            <% if (user) { %>
            <li><a class="dropdown-item" href="/upload">Subir Productos</a>
            </li>
            <% } %>
            <li><hr class="dropdown-divider"></li>
            <% if (user) { %>
              <li><a class="dropdown-item" href="/user/perfil">Mi Perfil</a>
              </li>
              <% } %>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="/users/logout">Cerrar sesión</a></li>
          </ul>
        </li> -->
        <% } %>
         <% if (!user) { %>
        <!-- <li class="nav-item">
          <a class="nav-link " href="/users/signin">Iniciar Sesión</a>
        </li>
        <% } else  if (user.email === process.env.EMAIL_ADMIN) { %>
        <li class="nav-item">
          <a class="nav-link" href="/users/signup">Registrar Usuario</a> 
        </li> -->
        <% } %>
        
      </ul>
    </div>
  </div>
</nav>
<% } %>